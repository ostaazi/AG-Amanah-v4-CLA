# Use the locally installed JDK for the Gradle daemon.
#
# The previous config pinned a JetBrains JDK 21 download via foojay.io. That
# breaks in restricted/sandboxed environments and is unnecessary for Android
# builds (AGP requires JDK 17+).
toolchainVendor=ADOPTIUM
toolchainVersion=17
