# تقييم شامل لـ SecurityCortex.kt v3.0+ (2153 سطر)

> **تاريخ التقييم**: فبراير 2026
> **الملف**: `android-child/app/src/main/java/com/amanah/child/utils/SecurityCortex.kt`
> **الحجم**: 2153 سطر (مقارنة بـ 1033 سطر في النسخة السابقة)

---

## أولاً: ما تم إضافته (ممتاز)

### 1. نموذج مشاهد العنف (Violence Scene Model)
- نموذج TFLite مستقل ثانٍ (`violence_scene_mobile.tflite`)
- وضع تقييم مزدوج مع معايرة احتمالات مستقلة
- حدود قابلة للتعديل (medium/high/critical/safeSuppression/marginGuard)
- حارس هامشي (margin guard) لمنع التصنيف في المنطقة الرمادية
- **تقييم**: ممتاز - يغطي فجوة حقيقية في كشف المحتوى العنيف المرئي

### 2. الحدود القابلة للتعديل عن بُعد (Remote-Configurable Thresholds)
- ~20 حداً (`@Volatile`) قابلة للتعديل عبر Firestore من لوحة الوالدين
- مخزنة في SharedPreferences للاستمرار بعد إعادة التشغيل
- تطبيق thread-safe بالمتغيرات `volatile`
- فحص اتساق (consistency enforcement) لمنع قيم متناقضة
- **تقييم**: قرار هندسي ذكي - يمنح الوالد مرونة عالية مع حماية من إعدادات غير منطقية

### 3. تحليل سياق نية المستدرج (Predator Context Intent Analysis)
- 7 عائلات فرعية: السرية، العزل، تحويل المنصة، طلب وسائط، لقاء، ضغط، إكراه
- تسجيل مرجّح (base 0.38 + أوزان لكل عائلة + مكافآت تركيبية)
- كشف التركيبات الخطرة (سرية + عزل + لقاء = إنذار فوري)
- حد أدنى 0.80 لتقليل الإيجابيات الكاذبة
- **تقييم**: عبقري - يكشف أنماط التصيد التي تفوت على الكلمات المفتاحية البسيطة

### 4. التطبيع الهيكلي (Skeleton Normalization)
- طبقة عميقة تحوّل الأحرف إلى فئات بصرية (0, 1, 3, 5)
- `purifyToSkeleton()` → يزيل كل التمويه البصري
- `mapSkeletonChar()` → يصنّف بالشكل لا المحتوى
- `detectObfuscatedSkeletonThreat()` → كشف التهديدات المموهة على مستوى الهيكل
- مجموعات خطر هيكلية + علامات فعل هيكلية
- **تقييم**: ابتكاري - تقنية نادرة حتى في المنتجات التجارية الكبرى

### 5. كشف الإصابات المُحسّن (Enhanced Injury Detection)
- تحليل مكونات متصلة بـ BFS (flood fill)
- كشف الحواف عبر تدرج الإضاءة (luminance gradient)
- 6 فلاتر لاستبعاد عناصر واجهة المستخدم:
  - `fragmentedUiPattern` - أنماط واجهة مجزأة
  - `weakRedUiPattern` - أحمر ضعيف من واجهة المستخدم
  - `fragmentedHighClusterUiPattern` - تجمعات مجزأة عالية
  - `broadWarmBackgroundPattern` - خلفيات دافئة عريضة
  - `uniformWarmUiPattern` - واجهة دافئة موحدة
  - `lowTextureHighDensityPattern` - كثافة عالية بدون ملمس
- تقييم ثقة متعدد العوامل
- **تقييم**: قفزة نوعية في جودة كشف الإصابات مع تقليل ممتاز للإيجابيات الكاذبة

### 6. بوابة حدود قواعد النصوص (Text Rule Threshold Gate)
- `applyTextRuleThresholds()` تطبق حدوداً ديناميكية لكل فئة
- مرونة عالية في ضبط الحساسية حسب الفئة
- تكامل مع نظام التحكم عن بعد
- **تقييم**: طبقة تحكم مهمة لموازنة الدقة والتغطية

---

## ثانياً: الفجوات الحرجة المكتشفة (8 فجوات)

### الفجوة 1: لا يوجد كشف إيموجي (False Negative)
- **المشكلة**: `normalizeForMatch()` تزيل جميع الإيموجي قبل التحليل
- **الخطورة**: عالية جداً
- **أمثلة تمر بدون كشف**:
  - `🍆💦` (إيحاء جنسي شائع جداً بين المراهقين)
  - `🔫🗡️💀` (تهديد بالعنف)
  - `💊🍃🌿` (إشارة لمخدرات)
  - `🤫📸` (سرية + صور = تصيد)
- **الحل**: إضافة `emojiThreatSequences` map مع تحليل تسلسلات الإيموجي قبل التطبيع

### الفجوة 2: لا يوجد Safe Context Whitelist (False Positive)
- **المشكلة**: كلمات عادية في سياقات آمنة تُفعّل الإنذارات
- **الخطورة**: عالية - تقلل ثقة المستخدم بالتطبيق
- **أمثلة إنذارات كاذبة**:
  - "blood test results" → كشف عنف
  - "killer app" → كشف عنف
  - "die hard movie" → كشف عنف
  - "kill the process" → كشف عنف
  - "nude color lipstick" → كشف جنسي
  - "xxx large size" → كشف جنسي
  - "breast cancer awareness" → كشف جنسي
- **الحل**: إنشاء `safeContextPhrases` map يحتوي عبارات آمنة مع نصف قطر سياقي (±3 كلمات)

### الفجوة 3: لا يوجد كشف سياق ألعاب (False Positive)
- **المشكلة**: محتوى ألعاب الفيديو يُصنّف كتهديد حقيقي
- **الخطورة**: متوسطة-عالية (الألعاب أكثر محتوى يتعامل معه المراهقون)
- **أمثلة إنذارات كاذبة**:
  - "headshot 360 no scope kill streak" → عنف حقيقي
  - "I'll kill you in the game" → تهديد حقيقي
  - "raid boss blood bath" → عنف حقيقي
  - أسماء ألعاب: "Call of Duty", "Mortal Kombat", "Dead by Daylight"
- **الحل**: إنشاء `gamingContextMarkers` (أسماء ألعاب + مصطلحات gaming) مع `gamingContextWeight` لتخفيض درجة الخطورة

### الفجوة 4: لا يوجد كشف مخدرات (False Negative)
- **المشكلة**: لا توجد فئة مخدرات/مواد محظورة في النظام
- **الخطورة**: عالية
- **أمثلة تمر بدون كشف**:
  - "weed", "edibles", "420", "blaze it"
  - "molly", "rolling", "tripping"
  - "xans", "percs", "lean", "sippin"
  - "حشيش", "بانجو", "كبتاجون"
  - الرموز المشفرة: "🌿", "gas", "loud", "fire"
- **الحل**: إضافة فئة `DRUGS` جديدة في `ThreatCategory` مع أنماط إنجليزية وعربية

### الفجوة 5: تغطية لهجات عربية محدودة (False Negative)
- **المشكلة**: الأنماط العربية تغطي الفصحى فقط تقريباً
- **الخطورة**: متوسطة-عالية (معظم المراهقين العرب يتحدثون بالعامية)
- **أمثلة تمر بدون كشف**:
  - المصرية: "هنروح مكان لوحدنا" (عزل)، "ابعتيلي صورتك" (طلب وسائط)
  - الشامية: "بدي شوفك" (لقاء)، "تعي عندي" (عزل)
  - الخليجية: "أبيك ترسلين" (طلب وسائط)
  - الفرانكو-عرب: "3ayz soretk", "ta3ali 3andi"
- **الحل**: توسيع `predatorSubFamilies` والأنماط الأخرى بلهجات مصرية/شامية/خليجية/مغربية + فرانكو-عرب

### الفجوة 6: لا يوجد كشف Homoglyphs و Zero-Width (False Negative)
- **المشكلة**: أحرف Unicode متشابهة بصرياً تتجاوز كل الكشف
- **الخطورة**: متوسطة (يستخدمها المتقدمون)
- **أمثلة تمر بدون كشف**:
  - `ѕеху` (Cyrillic с, е بدل Latin s, e) - تبدو مطابقة لـ "sexy"
  - `p​o​r​n` (zero-width spaces بين الحروف) - تبدو كـ "porn" لكن لا تطابق regex
  - `nⅰke` (Roman numeral ⅰ بدل i)
  - حروف fullwidth: `ｓｅｘ`
- **الحل**: إضافة `cleanHomoglyphs()` و `stripZeroWidth()` كأول خطوة في pipeline النصوص

### الفجوة 7: لا يوجد كشف نسبة جلد مكشوف (False Negative Visual)
- **المشكلة**: صور "suggestive" بدون عري كامل تتجاوز نموذج NSFW
- **الخطورة**: متوسطة
- **أمثلة**:
  - صور بملابس كاشفة جداً (لكن ليست عارية)
  - أوضاع إيحائية بملابس
  - صور تحت ملابس داخلية (lingerie)
- **الحل**: إضافة `estimateSkinExposure()` باستخدام YCbCr color space لكشف نسبة الجلد، ثم دمجه مع نتيجة NSFW

### الفجوة 8: لا يوجد كشف أرقام/حسابات تواصل (False Negative)
- **المشكلة**: مشاركة معلومات التواصل الشخصية لا تُكشف
- **الخطورة**: متوسطة-عالية (مؤشر قوي على التصيد)
- **أمثلة تمر بدون كشف**:
  - "رقمي 0551234567"
  - "add me on snap: username123"
  - "واتساب: +974 5555 1234"
  - "تلقاني في إنستا @user"
  - "DM me", "inbox me"
- **الحل**: إضافة regex للأرقام + أنماط حسابات التواصل الاجتماعي + ربطها بفئة CONTACT_SHARING جديدة

---

## ثالثاً: خطة التحسين (4 مراحل)

### المرحلة 1: القضاء على الإيجابيات الكاذبة (أعلى أولوية)
**التأثير**: يزيل ~90% من الإنذارات الكاذبة
**التعقيد**: منخفض-متوسط

| البند | التفاصيل | تقليل FP |
|---|---|---|
| Safe Context Whitelist | قائمة عبارات آمنة (blood test, killer app, nude color...) مع نصف قطر سياقي ±3 كلمات | ~70% |
| Gaming Context Filter | كشف سياق الألعاب (أسماء ألعاب + مصطلحات gaming) مع تخفيض درجة الخطورة | ~20% |
| Minimum Context Length | رفض النصوص أقل من 3 كلمات (تتطابق صدفة مع أنماط قصيرة) | ~5% |

### المرحلة 2: القضاء على السلبيات الكاذبة (أولوية عالية)
**التأثير**: يكشف ~40% محتوى إضافي كان يمر بدون كشف
**التعقيد**: متوسط

| البند | التفاصيل | تغطية جديدة |
|---|---|---|
| كشف إيموجي | `emojiThreatSequences` map + تحليل تسلسلات قبل التطبيع | +15% |
| فئة مخدرات | `DRUGS` category مع أنماط EN/AR + رموز مشفرة | +8% |
| لهجات عربية | مصرية/شامية/خليجية/مغربية + فرانكو-عرب | +10% |
| Homoglyphs + Zero-Width | `cleanHomoglyphs()` + `stripZeroWidth()` في بداية pipeline | +5% |
| كشف معلومات تواصل | regex أرقام + أنماط حسابات اجتماعية + فئة `CONTACT_SHARING` | +7% |

### المرحلة 3: تعزيز الكشف البصري (أولوية متوسطة)
**التأثير**: يكشف محتوى بصري إضافي
**التعقيد**: متوسط-عالي

| البند | التفاصيل |
|---|---|
| Skin Exposure Estimation | `estimateSkinExposure()` بـ YCbCr color space |
| NSFW + Skin Fusion | دمج نتيجة النموذج مع نسبة الجلد لقرار أدق |

### المرحلة 4: ميزات تنافسية عالمية (للتفرد)
**التأثير**: ميزات فريدة غير موجودة في المنافسين
**التعقيد**: عالي

| البند | التفاصيل |
|---|---|
| Conversation Context Ring Buffer | حلقة محادثة (آخر N رسالة) لكشف التصيد البطيء على مدى أيام |
| Per-App Risk Profiles | مستوى خطر مختلف لكل تطبيق (Snapchat > Calculator) |
| Behavioral Frequency Scoring | مضاعف إنذار يزداد مع تراكم الأحداث في فترة قصيرة |
| Slang Abbreviation Dictionary | قاموس اختصارات المراهقين المشفرة (GYAT, bussin, no cap, sus...) |

---

## رابعاً: الأولويات المقترحة

```
المرحلة 1 (FP Elimination) ← ابدأ هنا — أعلى تأثير، أقل تعقيد
    ↓
المرحلة 2 (FN Elimination) ← ثاني أعلى تأثير
    ↓
المرحلة 3 (Visual Enhancement) ← تحسين تكميلي
    ↓
المرحلة 4 (Competitive Features) ← تفرد عالمي
```

---

## 4.1 معايير Zero غير القابلة للتفاوض (Operational Definition)

> الهدف الاستراتيجي هو: `zero latency` + `zero false positive` + `zero false negative`.
> هندسياً، الوصول إلى "صفر مطلق" في FP وFN معاً غير واقعي رياضياً، لذلك نعتمد **Operational Zero** بقياسات صارمة تمنع الضرر التشغيلي.

### A) Zero Latency
- كل مسار كشف Hot Path يجب أن يبقى محلياً 100% (بدون نداء شبكة متزامن).
- ممنوع أي blocking I/O داخل دورة المسح.
- ميزانية الأداء على جهاز مرجعي متوسط:
  - `p50 <= 80ms`
  - `p95 <= 120ms`
  - `p99 <= 180ms`
- أي ميزة جديدة تتجاوز الميزانية = فشل قبول.

### B) Zero False Positive (Operational)
- ممنوع تنفيذ إجراءات قاسية (Lock / Blackout / Siren) إلا عبر:
  - إجماع كاشفين مستقلين (مثلاً visual + text/context) **أو**
  - درجة حرجة مع هامش ثقة أعلى من `marginGuard`.
- المنطقة الرمادية تتحول إلى Soft Alert فقط (بدون قفل تلقائي).
- على مجموعة Safe Regression المعتمدة: `FP_critical = 0` كشرط إطلاق.

### C) Zero False Negative (Operational)
- Pipeline متعدد المراحل إجباري:
  1) تطبيع صلب (emoji + homoglyph + zero-width + skeleton)
  2) كشف نمطي/سياقي
  3) دمج القرار (fusion) مع قواعد الفئات
- أي فئة حرجة جديدة (مثل DRUGS/CONTACT_SHARING) لا تُطلق بدون اختبار Recall مخصص.
- على مجموعة Red-Team الحرجة المعتمدة: `FN_critical = 0` كشرط إطلاق.

### D) بوابات قبول أسبوعية (Quality Gates)
- يمنع نشر أي تعديل إذا فشل أحد الشروط:
  - Performance budget fail
  - `FP_critical > 0` على Safe set
  - `FN_critical > 0` على Critical set
  - انهيار تفسير الدليل (reason/message ثابت غير مرتبط بالمدخل)

---

## خامساً: إضافات المستخدم الأخرى (تمت مراجعتها)

### ScreenGuardianService.kt
- نظام Evidence Bundle Telemetry لتتبع اكتمال حزم الإنذارات
- طابور إعادة محاولة تحميل الإنذارات (Alert Upload Retry Queue) مع exponential backoff
- `resolveSelfThreatIgnoreReason()` (بديل أفضل عن `shouldIgnoreSelfThreat()`)
- `PendingAlertUpload` data class
- `uploadAlertPayload()`, `flushPendingAlertUploads()`, `queueAlertRetry()` مع معالجة الفشل النهائي

### RemoteCommandService.kt
- `handleVisualThresholdsCommand()` → `SecurityCortex.applyVisualThresholdOverrides()`
- `handleTextRuleThresholdsCommand()` → `SecurityCortex.applyTextRuleThresholdOverrides()`
- أمر `blockCameraAndMic` جديد

### ThreatSyncManager.kt
- مزامنة فورية من Firestore لـ `threat_dictionary`
- تخزين محلي في SharedPreferences (pipe-delimited) للعمل offline
- يغذي `SecurityCortex.updateThreats()` بكل تحديث

---

## سادساً: ملاحظات فنية عامة

1. **الأداء**: كل العمليات محلية 100% (TFLite + regex + heuristics) — zero-latency محقق
2. **Thread Safety**: استخدام `@Volatile` صحيح للحدود القابلة للتعديل
3. **Offline**: يعمل بالكامل بدون إنترنت بعد التحميل الأول للأنماط
4. **كود نظيف**: تنظيم جيد بفصل واضح بين الوظائف
5. **قابلية التوسع**: البنية الحالية تدعم إضافة فئات وأنماط جديدة بسهولة

---

> **الخلاصة**: التحسينات المضافة ممتازة ومتقدمة تقنياً. سد الفجوات الـ 8 المذكورة أعلاه سيحول SecurityCortex إلى أحد أقوى محركات الكشف المحلية على مستوى المنتجات العالمية.
